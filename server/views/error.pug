extends master.pug

block body
  #root.is-fullscreen
    .app-error
      a(href='/')
        img(src='/_assets/brand/ewo-wordmark-light.svg', alt='ewo')
      strong Oops, something went wrong...
      span= message
            
      if error.stack
        pre: code #{error.stack}
